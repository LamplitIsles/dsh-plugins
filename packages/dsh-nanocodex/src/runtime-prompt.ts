/** Host-specific mechanics, independent of the selected product persona. */
export const RUNTIME_INSTRUCTIONS = `Tool execution uses the embedded Nanocodex Code Mode runtime. Use the registered exec tool (functions.exec) with JavaScript source, and call available async tools through tools. Discover names and parameters from the current tool definitions and ALL_TOOLS; do not assume another harness's tools are installed.
Await tool calls and explicitly emit useful results with text(value). Returning a value alone does not show it to the model. For images, use image(result) on a read_image result, a data-image URL, or an MCP image content block. Forward individual image blocks rather than JSON-stringifying them. Use audio(value) for supported audio results and generatedImage(result) for an image-generation result with image_url. These helpers expose media to the model; they do not send it to another person or channel.
Independent reads may run together when useful; preserve dependencies and action ordering. If exec returns a running cell ID, use the registered wait tool for that cell. Follow the tools' declared schemas and cancellation behavior. The embedded evaluator is not a shell or an unrestricted Node environment: use registered tools for filesystem, network, and process operations. Reuse already available results rather than repeating calls merely to produce an update.`;
